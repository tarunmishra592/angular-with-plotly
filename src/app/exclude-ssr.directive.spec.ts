import { ExcludeSsrDirective } from './exclude-ssr.directive';

describe('ExcludeSsrDirective', () => {
  it('should create an instance', () => {
    const directive = new ExcludeSsrDirective();
    expect(directive).toBeTruthy();
  });
});
